[pytest]
testpaths = tests
markers =
    pg: braucht ein echtes Postgres (Wegwerf-Instanz oder BABU_TEST_DB_URL).
        Ohne eines wird der Test übersprungen — die Suite darf nirgends
        Postgres voraussetzen.
