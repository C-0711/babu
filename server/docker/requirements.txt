# Stand des Server-venv vom 27.08.2026 (~/belegreview/.venv) — gepinnt,
# damit das Image reproduzierbar ist. Beim Heben einer Version: Suite fahren.
fastapi==0.141.1
uvicorn==0.52.1
requests==2.34.2
python-multipart==0.0.32
pypdfium2==5.12.1
pillow==12.3.0
numpy==2.5.2
# Postgres-Treiber für die Datenbankschicht (Plan 21, Phase 1), 02.09.2026.
# [binary] statt Quellbau: bringt libpq selbst mit, das Image braucht damit
# kein libpq-dev. Ohne BABU_DB_URL wird es nie importiert — der SQLite-Weg
# läuft auch, wenn das Paket fehlt.
psycopg[binary]==3.3.5
