# Statische Seiten von babu.0711.io

Diese Dateien liegen auf dem Server unter `~/babu-web/` und werden von
`belegreview/babu_web.py` ausgeliefert:

| Datei          | Route       | ausgeliefert von      |
|----------------|-------------|-----------------------|
| `index.html`   | `/`         | `SEITE` (Landing)     |
| `einkauf.html` | `/einkauf`  | `einkauf_seite()`     |

Die Landing wurde bis zum 21.08.2026 **nur auf dem Server** gepflegt und
war nirgends versioniert — sichtbar an den Backups `index.html.vor-*` im
Serververzeichnis. Seither ist sie hier. Wer sie ändert, ändert sie hier
und spielt sie ein, nicht umgekehrt.

Nicht enthalten: `bilder/`, `app/` (IPA und Manifest) und `box/` — die
gehören nicht ins Repo.
