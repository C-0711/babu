Repository: /Users/christophbertsch/babu/.claude/worktrees/project-handover-context-7bfaa2 (FastAPI server/belegreview/babu_web.py ~9586 lines, boxschreiber.py, portal.html; docker: server/docker/compose.yml, Dockerfile; babu-web runs as one container with host network on H200V; SQLite PORTAL_DB ~/babu-web/portal.db; the Belegbox is a bare git repo behind gateway insp-app :7808 which is OUT OF SCOPE / "nie anfassen"). Read-only; produce a detailed, phased implementation plan.

USER REQUEST: "Ich brauche einen Pro-Zugang für Steuerberater, die im Backend hunderte Mandanten verwalten können" + "deploy Postgres if necessary, and all in Docker if not already done".

FACTS FROM EXPLORATION (trust these; line numbers refer to babu_web.py unless noted):
Identity: wer_token(token) :290 (PAT→gitchain whoami, un lowercase); app_schluessel_pruefen :315; wer(request) :331; angemeldet :418; session cookie "babu_sitzung" = base64(un|exp)+HMAC (:348-388), carries ONLY un. Login routes: POST /api/anmelden :1157 (PAT, gated by env allowlist ERLAUBT :250), POST /api/login :1215 (email+pw scrypt, returns {un, rolle, box}), /api/app-anmelden :1248, /api/passwort :1283, /api/ich :1313 {un, rolle, box}. /api/signup :2666 creates nutzer with box=False.
DB: sqlite, schema created on every _db() call (:56-217), _DB_LOCK :53. Tables all keyed by bare `un TEXT`: lesestatus, einstellungen(PK un,schluessel), registrierungen, nutzer(email PK, name, salon, rolle, pw, aktiv, angelegt, letzter_login, gehoert_zu, box), abschluss_status(PK un), team, termin, kundin, behandlung, leistung, mitarbeiter, wa_faden, wa_nachricht, gespraech, nachricht, app_schluessel, anlagegut, einladung. db_einstellungen(un) :236; salon_von(un) :3156 (= gehoert_zu or un); inconsistent use of db_einstellungen(un) vs db_einstellungen(salon_von(un)) (17 raw call sites); kontenrahmen_von(un) :2810.
Roles: NUTZER_ROLLEN=("admin","kanzlei","salon","mitarbeit") :436; rolle(un) :3149 falls back to "kanzlei" when env BABU_ROLLEN empty (fail-open; prod sets christoph0711.io:kanzlei in compose.yml:24); darf_verwalten :3176 = rolle in (admin,kanzlei); _verwalter_wache :3180; box_mitglied(un) :510 returns bool (kanzlei/admin → True). Guards: _api_wache :1126, _box_wache :1141 (104 routes).
Verwaltung routes (all global, no scoping): GET /api/nutzer :3193 (all accounts), POST /api/nutzer :3211, POST /api/nutzer-aktion :3231 (deaktivieren/aktivieren/rolle/box_freigeben/box_sperren/passwort_neu :3266 returns plaintext start password — cross-tenant takeover risk, no audit), GET /api/registrierungen :2714, POST /api/registrierung-einrichten :3275, POST /api/team-zugang :8657 (mitarbeit under caller's un). Frontend: menu #menu-verwaltung (portal.html:923, shown at 3416), view a-verwaltung :1397, ladeVerwaltung :4970 flat list, meineRolle global :3355; no-box state a-gesperrt :934 says box is created by hand (mailto).
Single-box globals: STORE :40 (used by _git :568, git_show :527, _blobs_lesen :575, review_pfad :540); _INDEX/_INDEX_LOCK/INDEX_TTL :555-563, _index_bauen :721, index_aktuell :1014, ~20 unconditional invalidations `_INDEX["geprueft"]=0.0`; _BLOB_STAND :5727, _SEITEN_CACHE :5755, _BELEG_VEKTOREN :2139, _MELDUNGEN_CACHE :5150; locks _RECHNUNG_SCHLOSS :560, _TERMIN_SCHLOSS :562, _WA_SCHLOSS :7205, _NACHTRAG_LOCK :5055, _ABSCHLUSS_LOCK :4066, _LLM_SEMAPHORE(1) :4202, _ABSCHLUSS_JOBS :4065, temp dirs ABSCHLUSS_TMP :4062, AUSWERTUNG_TMP :4635; BABU_REF literal "inspektor/ws-christoph0711.io/babu" :1689,1706; BABU_MANDANT/extf.BERATER single env (:3353). boxschreiber.py: KLON :23 (one working copy), GATEWAY :24, REF :25, PAT_PFAD :26, REMOTE :27, _SCHLOSS :38 global lock; schreiben(dict|path, inhalt, msg, un) :113, loeschen :135, _bereit() fetch+reset --hard :60, _commit_und_push :83. compose.yml mounts ~/inspektor-store :ro and the PAT file; writes go via gateway :7808. Box creation today: manual (insp-app + UPDATE nutzer SET box=1). Docs aspire to "Steuerberater-Portal mit Multi-Mandanten-Queue", "Mandanten-Scoping", "Row-Level-Security" (docs/build-plan.md:31,48,99).
Tests: /tmp/babu-venv/bin/python -m pytest tests/ (1608 green); welt fixture in tests/test_mehrseiten_buendel.py monkeypatches babu_web.STORE, PORTAL_DB, boxschreiber.KLON/REMOTE/PAT_PFAD, _box_wache. iOS app talks to the same API with a device key; /ablage returns BABU_REF.
Security rules of the project: never accept/print token values; new routes via _box_wache/box_mitglied, never ERLAUBT; insp-app never touched.

DESIGN GOALS: a Kanzlei ("Pro") account manages hundreds of Mandanten: list/search/paginate Mandanten, open one Mandant ("acting as") and see everything the owner sees (Belege, Kassenbuch, Verträge/Team costs in BWA, Export with the Mandant's Kontenrahmen and Berater/Mandant numbers), a cross-Mandant work queue (open Rückfragen, Monate ohne Freigabe, Export fällig), invite/onboard a Mandant, per-Kanzlei scoping of /api/nutzer and actions, audit log of Kanzlei actions, no plaintext password reset across tenants (invite/reset link instead). Owner (salon) accounts unchanged.

PLAN REQUIREMENTS:
1. Data model: propose tables (Postgres): kanzlei, mandant (id, name, box_ref, kontenrahmen, berater_nr, mandant_nr, status), kanzlei_mandant (membership/role), nutzer gains mandant_id, audit_log. Decide: migrate ALL 18 sqlite tables to Postgres now vs. hybrid (Postgres for tenancy/auth/audit, sqlite stays per-mandant?). Recommend one, with a migration path and a DB access layer that lets `_db()` callers switch (psycopg3 vs sqlite3 differences: placeholders ? vs %s, `INSERT OR REPLACE`, `PRAGMA`, `CREATE TABLE IF NOT EXISTS` on every call). Estimate blast radius (count SQL call sites with grep patterns you can suggest).
2. Docker: compose.yml adds service postgres:16 with named volume, env for DSN (BABU_DB_URL), healthcheck, backups (pg_dump cron in container or host), keep host network for babu-web or move both into a compose network — decide. Dev/test: tests must still run without Postgres (sqlite fallback or testcontainers?). Migration script portal.db → Postgres (idempotent, run once).
3. Box context: introduce a `Box` object (store path, ref, klon dir, lock) resolved per request: box_von(un, mandant_id) → replaces STORE/boxschreiber constants; per-box _INDEX registry with LRU + TTL, per-box locks, per-box _BELEG_VEKTOREN etc. Which functions change signature (list the hot ones: _git, git_show, _blobs_lesen, index_aktuell, boxschreiber.schreiben/loeschen, _beleg_vektoren, historie_lesen). Propose a request-scoped context (contextvar) so 104 _box_wache routes need minimal edits: _box_wache returns (un, fehler) today — extend to set the active Box in a contextvar; routes keep calling index_aktuell() unchanged.
4. Acting-as: cookie/session gains `mandant` claim OR a header X-Mandant checked against membership; salon_von(un) resolution when acting as a Mandant (→ the Mandant's owner un, so Team/Termine/Einstellungen/kontenrahmen resolve correctly); explicit list of the inconsistent db_einstellungen(un) call sites to normalise.
5. Provisioning: box creation cannot be automated here (insp-app is off-limits). Propose: `mandant` row status "box_ausstehend" + a manual step documented; the Kanzlei UI shows "Belegbox wird eingerichtet". (Mark as a decision the user must confirm.)
6. Frontend (portal.html): Mandanten view for Kanzlei (table with search, pagination, status chips, open Rückfragen count), a Mandant switcher in the header (chip "Mandant: Salon X · wechseln"), work queue view, scoped Zugänge; iOS app unchanged (owner only).
7. Security: audit_log on every kanzlei action, remove passwort_neu plaintext for cross-tenant (replace by reset link/one-time code), rate limits, fail-closed role default (rolle() fallback "salon" not "kanzlei" — check prod env impact).
8. Phasing: Phase 0 (tenancy tables + Box context + acting-as with the existing single box; no Postgres yet? or Postgres first?) — recommend an order that ships value early and keeps the suite green each step; list tests to add per phase (welt fixture with two boxes).
Deliver a concrete plan: files/functions to change, new modules (e.g. server/belegreview/mandanten.py, db.py), schemas, route list, compose diff, migration steps, rollout/deploy ritual notes (Golden diff must stay byte-identical for the owner), and verification. Concise, decision-oriented, no alternative surveys except genuinely open decisions (mark them).