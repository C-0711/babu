Repository: /Users/christophbertsch/babu/.claude/worktrees/project-handover-context-7bfaa2 (FastAPI server/belegreview/babu_web.py, monatsabschluss.py, gemma_buchung.py, portal.html single-file frontend with hash-routed views; iOS app ios/Beleg; tests via /tmp/babu-venv/bin/python -m pytest tests/, 1608 green; UI language German, no tech vocabulary in UI). Read-only; produce a concrete implementation plan for the 26 review findings below, in the user's priority order: first 1+2+3 (numbers agree everywhere, printed tax wins), then 4 (stuck state), then 6 (Kassenbuch + Rechnung on the desktop), then 5 (desktop layout), then the rest (P2, P3).

FINDINGS + LOCATED CODE (trust these):
P0-1 Three expense sums. Auswertung›Ausgaben = ladeZahlen portal.html:2399 ← GET /api/monat/{monat} babu_web.py:3469 → _monat_summen :1522-1557 (brutto of ALL Belege, grouped by belegart, fallback "Sonstiges" :1532). Monatsabschluss = ladeAbschluss portal.html:3592 ← GET /api/monatsabschluss/{monat} :8784 → monatsabschluss.bwa (monatsabschluss.py:294-399): netto by konto_skr04→KOSTENGRUPPEN, + Vertragskosten (:328-345 via vertraege_fuer_monat :253, fed by babu_web.vertraege_aktuell :8549) with anzahl 0 and e["aus_vertrag"] set but renderer portal.html:3634-3637 prints "0 Belege"; + personal_monat (team_personalkosten :8584). Heute "Bleibt dir" = ladeBleibtDir portal.html:1665 ← bwa.ergebnis. Category bug: _review_aus_einschaetzung babu_web.py:2025 writes category into einschaetzung.belegart but hard-codes "semantik": None (:2070, :2078); index reads display category only from review["semantik"]["belegart"] (:791) → app-path receipts show "Sonstiges" in Ausgaben, while Monatsabschluss groups by konto_skr04 (:793) → "Werbung, Bewirtung und Reisen".
P0-2 Vorsteuer: _review_aus_einschaetzung :2030-2039 computes netto=brutto/(1+satz), ust=brutto-netto from betrag_eur+ust_satz and DISCARDS buchung["steuersaetze"] (built by gemma_buchung._steuertabelle :492-509 from positions); writes summenprobe_ok None (:2059) so vorsteuer_monat (monatsabschluss.py:163-196) trusts ust verbatim. iOS: FeldParser.swift:133-141 parses printed Netto/USt/Brutto into steuerPositionen (summenprobeOK=true) but Store.gemmaBuchungAnwenden (Store.swift:447, 467-472) overwrites netto/ust with brutto/(1+satz); steuertabelleAnwenden (:483-490) only applies Gemma's steuersaetze if they sum to brutto ±0.02. Prompt rules: gemma_buchung.py:139-143 Summenschema, :113-117 "Rechne NIE 19 % aus einem Bruttobetrag heraus…", no Pfand rule, SCHEMA :167-183.
P0-3 Export "Summe 0,00": portal.html:3884-3885 reads d.summe but /api/monat returns "brutto" (:1554). Also the CSV uses status filter ("geprüft","exportiert") :3339 — the view should say so.
P0-4 "Wird gelesen" stuck: _status_ableiten :645-660 (erfasst/geprüft/nachfrage/exportiert); portal text statusSatz :1501, statusMarke :1807, detail hint :1990/:2011. Only /api/aufnahme with app ergebnis writes review/<stamm>.json (:1939-1968); POST /api/hochladen (:1720) and /ablage (:1629) write only docs/… → stuck forever; no watcher (deleted by policy, README:22-27: do not recreate). Escape hatches: POST /api/angaben/{stamm} :8480 (any save flips to geprüft via :831-833), POST /api/beleg/{stamm}/loeschen :1760, POST /api/buchung/einschaetzung :3483 (app-only JSON, no review write). Server has Gemma multimodal (gemma_buchung._gemma(prompt,bild,system) :525) and PDF text (abschluss_lesen.seiten_text). Project Zielbild: ONE reading on the iPhone (Vision) → einschaetzung → aufnahme; the portal upload path has no reading at all.
P1-5 Layout: main has no max-width (portal.html:92); .spalten grid (:105-117, @media min-width:1180px) is defined but never used; two-column only #detail-grid (:472) and #unterlage-grid (:258), .ablage-flaeche (:170); breakpoints 640/700/820/900/1180.
P1-6 App-only: Rechnungen text ladeRechnungen portal.html:4259 — APIs exist: POST /api/rechnungen :6035, POST /api/rechnung/{nummer}/pdf :6107, /bezahlt :6137, /storno :6171, GET /api/rechnungen :6015 (PDF rendering is app-side RechnungPDF.swift; portal has Briefkopf view a-briefkopf :1243). Kassenbuch text ladeAbschluss :3603-3605 — route POST /api/kassenbuch :9337 (datum in JSON body; fields KASSENBUCH_ZAHLEN, KASSENBUCH_NOTIZEN, trinkgeldVerteilt, korrekturen, grund; GoBD guard kassenfest.darf_schreiben :9394, 409 when festgeschrieben), GET /api/kassenbuch/{datum} :9317, question set = monatsabschluss.umsatz_profil()["fragen"] already returned as profil by /api/monatsabschluss. Termine: GET /api/termine?von&bis :6453 supports ranges; portal only requests one day (ladeTermine :4422, :4427, stepping :4909). Salon-Check grey card saloncheck._grau :53-58, rendered read-only scReport :3269-3282; write-back only scVorschlaege→POST /api/abschluss/uebernehmen :4957 (Stammdaten).
P1-7 Ablage names: _ablage_eintraege :5627; auszuege/ titel = beiakte titel or raw filename (:5680-5686); kontoauszug.parse_text returns {konto, monat, umsaetze} stored as .umsaetze.json (:2534-2538, :1925-1927); month = folder; seiten computed :5668; bank name NOT extracted (parse_text reads only account no.). Filenames from boxschreiber.beleg_dateiname :188.
P1-8 Delete: ablageWerkzeug portal.html:2707-2717 (loeschbar && !mitarbeit); loeschbar set :5640/:5652/:5719; handlers dokumentLoeschen :3960, auszugLoeschen :3970, confirm sheet loeschFragen :3907; routes /api/dokument-loeschen :2310, /api/auszug-loeschen :2351; Beleg delete /api/beleg/{stamm}/loeschen :1760.
P2: week labels wochenGruppe :1823-1833 used in ladeBelege :1851; Heute letzte Belege /api/belege?limit=3 :1770, sorted by hochgeladen in _beleg_liste :1034; duplicate text built in /api/aufnahme :1953-1958 into felder.offen, no resolve route (cleared by /api/angaben via _offen_nach_angaben :663-687 or delete); Post Frist raw ISO ladePost :2940 (datumDeutsch helper exists :3857); Verträge "Frist steht im Vertrag" ladeVertraege :4311-4314; heading :1108; un without @: GitChain usernames have no @ (wer_token :290), shown :3354/:3414, :3358 hides password field for non-@; Team form ladeTeam :3431 fields :3487-3505, "Speichern" uses class="hin" styled only under .nachfrage (:424) → unstyled, same at :4995/:5035; kw-kuendigen checked :3715; sub-tab bars :968-973/:1034-1039/:1047-1052; two personal views a-personal :1338 (ladePersonal :4627, /api/mitarbeiter :6694) vs a-team :1124 (ladeTeam :3431, /api/team :8590; only team feeds team_personalkosten→BWA); kennzahlKacheln :2365-2397 ("0 Sek." :2370, kennzahlen_monat :3400-3409); Fragen view markup :1422-1437, CSS :599-609.
P3: GET /portal :1048 FileResponse unauthenticated, ~250 KB single file; HTML_FRISCH no-cache :1040; 16 <label> total; no Impressum/Datenschutz in portal (start.html:153 has a Belehrung); Zugänge routes ARE server-guarded by _verwalter_wache :3180 (darf_verwalten :3176) — finding 26 is satisfied; verify only.

PLAN REQUIREMENTS: For each finding give the concrete change (file, function, what), the chosen definition where definitions conflict (P0-1: recommend ONE canonical expense number per view family — e.g. Ausgaben view shows netto by KOSTENGRUPPEN like Monatsabschluss, with an explicit "davon aus Verträgen ohne Beleg 905 €" line; Heute explains "Bleibt dir" with a one-line breakdown; category derived from konto_skr04 everywhere via a single helper), the P0-2 rule (printed tax lines win: server keeps steuersaetze from einschaetzung, iOS applies OCR steuerPositionen when Gemma's table is missing; prompt gets a Pfand/deposit rule + a mixed example; summenprobe_ok computed on server), the P0-4 design (portal upload path gets a server-side reading: PDF text or Gemma multimodal → einschaetzung → review write, in a background thread with a time cap; plus a timeout state "Konnte nicht gelesen werden — magst du drauf schauen?" after N minutes with a "Nochmal versuchen" and "Selbst eintragen" action, using existing /api/angaben; respect the no-watcher rule by making it request-triggered, not a poller), P1-6 (portal Kassenbuch form for the day using profil.fragen + POST /api/kassenbuch with grund handling and 409 message; portal Rechnung form posting to /api/rechnungen + PDF via /api/rechnung/{nummer}/pdf; Termine week view using von/bis; Salon-Check grey card gets an input + a small route to store the corrected value in abschluss_status), P1-5 (apply .spalten to Heute/Belege/Termine; Belege as sortable table on ≥1180px with list+detail split), P1-7 (title synthesis "Kontoauszug Juli 2026 · <Bank>" — bank from IBAN/BIC/filename or parse_text extension), P1-8 (delete moved into detail/context menu, confirm keeps), and the P2/P3 items as short bullets each (P3-21: serve a login shell and load the app bundle after auth — sketch minimal split of portal.html into portal-login.html + portal-app.html served behind _api_wache; P3-22 cache headers with content hash; P3-25 Impressum/Datenschutz footer with placeholder content the user must supply — mark as user input needed).
Also list tests to add/adjust per item (test_monatsabschluss, test_gemma_buchung, test_schreiben/welt route tests, ios/Tests harness for FeldParser/Store), and a verification section (portal-vorschau: BABU_ORIGIN=http://localhost:7899 /tmp/babu-venv/bin/python werkzeuge/portal-vorschau/portal_vorschau.py; login via fetch("/api/anmelden",{pat:"test-pat"})). Be concrete and concise; group into 4 delivery batches matching the priority order.