Repository: /Users/christophbertsch/babu/.claude/worktrees/project-handover-context-7bfaa2 (FastAPI server/belegreview/babu_web.py, kompendium.py, gemma_buchung.py, portal.html; docker in server/docker/compose.yml; vLLM Gemma :11435, EmbeddingGemma :11436 on host H200V). Read-only; produce a detailed implementation plan (German UI wording, code identifiers may be German as in the repo).

USER REQUEST (translated): "All SKR04 accounts must be fully read out and available in the context via embeddings; and all DATEV topics must be uploadable in frontend and backend, sorted (by topic), and enter the context (chat + booking prompt)."

FACTS FROM EXPLORATION (trust these):
- kompendium.py: VERZEICHNIS=~/kompendium (container: /data/kompendium, mounted :ro in compose.yml:36). Files: vektoren.npy (fp32, L2-normalised, memmap), atome.jsonl (keys id, quelle, loc, text), grundwissen.md, kontierung-grundwissen.md. _laden() line 31 requires len(jsonl lines)==vektoren.shape[0] else the whole Kompendium is silent. suchen(frage_vektor,k) brute-force cosine, no threshold; atom(nr); grundwissen(); kontierungswissen(). NO build script in the repo; corpus (89,760 atoms from 182 sources incl. SKR04 PDF, AfA, BMF, juris) exists only on H200V.
- Embedding: babu_web.embedding_rechnen(text, als_dokument=True) line 2113 → POST EMBED_API {"model","input","truncate_prompt_tokens":2040}; doc prefix "title: none | text: …"[:6000], query prefix "task: search result | query: …"[:2000]. _LLM_SEMAPHORE=Semaphore(1) line 4202 — never flood :11435/:11436 from handlers.
- Consumers: chat POST /chat line 3907 uses _recherche(frage) line 2172: kompendium.suchen k=5, score>=0.30, no source filter, renders "[quelle · loc] text[:600]"; plus own-Beleg vectors _beleg_vektoren() line 2142 (HEAD-keyed matrix built from review/*.embedding.json, per-row normalised; threshold 0.35, top 3, git_show review/<stamm>.md[:900]). Booking: gemma_buchung.nachschlagen(zeilen, markdown, k=6) line 260 — query "Nutzungsdauer und Kontierung im Friseursalon: "+_sachwoerter; filter score>=0.40 AND quelle contains one of NACHSCHLAG_QUELLEN=("afa","kontenplan","skr04","skr03","bmf","ustg","estg","kontenrahmen","steuerschluessel") line 239; max 2 atoms, 400 chars each, injected in the variable prompt part. system_text() line 198 holds kompendium.kontierungswissen() in the byte-stable prefix.
- Per-Beleg sidecar pattern: review/<stamm>.md + review/<stamm>.embedding.json {"modell","dim","vektor"} written in the same boxschreiber.schreiben(dict) commit (babu_web.py:1961-1973); backfill_embeddings.py does it for old ones.
- Uploads: POST /api/dokumente line 2390 api_dokument_hochladen(request, name, titel, art) writes dokumente/{YYYY-MM}/{name} + .meta.json in one commit and dispatches a background thread by art (2424-2432). ABLAGE_ARTEN dict line 5551 (12 Fächer: beleg, behoerde, kanzlei, vertrag, abschluss, kontoauszug, export, kassenbuch, rechnung, historie, ustva, bwa); ABLAGE_PFAD_RE line 2231 must include any new top-level prefix; _ablage_eintraege() line 5627 walks git ls-tree with a prefix→art chain (5670-5711); .meta.json sidecars carry fach/titel. DOKUMENT_ENDUNGEN={.pdf,.jpg,.jpeg,.png} line 2224; HOCHLADEN_MAX 40 MB. PDF text: abschluss_lesen.seiten_text(pfad) (pypdfium2) line 79; scans → Gemma multimodal via abschluss_lesen.llm_json/_bild_nachricht. einsortieren.py has a weighted keyword classifier MERKMALE/entscheiden(text) (lines 21,150) as a pattern for topic sorting.
- Portal Ablage JS: ladeAblage 2604, ablageBaumZeichnen 2635, ablageKachel 2676, ablageWerkzeug 2707, ablageSuchen 2764 (keyword only, GET /api/ablage/suche line 5879), upload helper hochladenMitBalken 4105, existing wirings e.g. Vertrag 2894-2904 (/api/dokumente?art=vertrag&name&titel). ABLAGE_SYMBOL icon map.
- Data already in repo: server/belegreview/skr04_konten.py (KONTEN: 1516 {nr: name}), skr04_automatik.py (AUTOMATIK: 183 {nr: (AV|AM, satz, name)}), kontierung.KATEGORIEN (babu's ~37 categories with skr04/skr03 accounts and names). Local DATEV sources on the Mac: ~/JennyfromtheBlock/datev/*.pdf + hilfe-center/*.md (Dok 0907048 Steuerschlüssel, 0907108 Kontenrahmenänderungen).
- Tests: /tmp/babu-venv/bin/python -m pytest tests/ (1608 green). Tests for kompendium use a tmp VERZEICHNIS fixture (tests/test_kompendium.py:24-31).

DESIGN QUESTIONS TO RESOLVE IN THE PLAN:
1. SKR04 accounts as atoms: one atom per account with rich text (number, name, AV/AM + rate, Kontenklasse/Bereich, babu category if any, SKR03 counterpart if known from kontierung). Where do they live? Option A: extend the read-only host Kompendium via a NEW build script in the repo (werkzeuge/kompendium/konten_atome.py run on H200V host; append to atome.jsonl + vektoren.npy keeping the invariant; quelle="skr04-konto" so NACHSCHLAG_QUELLEN matches with zero code change). Option B: a second, writable "Wissen" store. Recommend, and design the atom text so that both "Welches Konto für Bewirtung?" and "Was ist Konto 6640?" retrieve well (consider a short "Frage-Form" line per atom). Also consider grundwissen: should a compact "Kontenübersicht" (the ~50 babu accounts + names) be added to kontierung-grundwissen.md (stable prefix) — mind prefix-cache size.
2. DATEV document uploads ("alle DATEV-Themen"): a new Ablage-Fach "Wissen" (or "DATEV-Wissen") with topic sorting (Themen: Kontenrahmen, Steuerschlüssel, Buchungsstapel/Schnittstelle, Anlagen/AfA, Umsatzsteuer, Lohn, Jahresabschluss/EÜR, Sonstiges) — auto-classified by keywords (reuse einsortieren-style MERKMALE) with manual override via .meta.json (reuse /api/ablage/umbenennen & verschieben patterns). Storage path proposal: wissen/<thema>/<datei> + <datei>.md (extracted text) + <datei>.atome.json (chunks with vectors) — design the chunking (e.g. ~800-token paragraphs with loc = page/heading), embedding done in a background thread under _LLM_SEMAPHORE with progress state visible in the Ablage card ("wird eingelesen … n/m"), scans via Gemma multimodal only when no text layer. In-process matrix like _beleg_vektoren (HEAD-keyed), merged into a common search function used by BOTH _recherche (chat) and nachschlagen (booking; quelle = "wissen:<thema>" and add "wissen" to NACHSCHLAG_QUELLEN) — with source attribution "[DATEV Dok 0907048 · S. 3]" in chat answers. Also backend upload path (CLI/rsync-free): the same route callable with curl/PAT; and a bulk import script for the Mac folder ~/JennyfromtheBlock/datev.
3. Frontend: Ablage tree gets Fach "Wissen" with Thema sub-nodes; upload chip "DATEV-Dokument ablegen" with Thema select; card shows Thema + "n Absätze eingelesen"; the chat shows "Nachgeschlagen in: …" chips. Keep UI wording non-technical per project rule (no "Embedding", say "Nachschlagewerk"/"Wissen").
4. Tests: kompendium fixture extension, unit tests for chunking/classifier, route test with welt fixture (tests/test_mehrseiten_buendel.welt), nachschlagen filter test.
5. Ops: what runs where (host script vs container), semaphore, size limits, and a rollout order.

Deliver: a step-by-step implementation plan with files/functions to add or change (exact names), data formats, the search-merge API signature, test list, and a verification section. Be concrete and concise; no alternatives except where a decision is genuinely open (mark it).